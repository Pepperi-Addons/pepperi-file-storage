{
  "name": "pfs-lambda-handler",
  "version": "1.0.0",
  "description": "",
  "main": "./index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "build2": "tsc && npm run zip-dist",
    "zip-dist": "npm run create-zip-dist && cd ./dist && zip  -j -FS -r ../zipped-dist/pfs-lambda-handler.zip . && cd ..",
    "create-zip-dist": "mkdir -p  zipped-dist",
    "pack": "npm run build && npm-pack-zip "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pepperi-addons/addon-infra-sdk": "file:../../AddonInfaSDK/pepperi-addons-addon-infra-sdk-3.0.0.tgz"
  },
  "bundledDependencies": [
    "@pepperi-addons/addon-infra-sdk"
  ],
  "devDependencies": {
    "npm-pack-zip": "^1.3.0"
  },
  "files": [
    "build"
  ]
}
